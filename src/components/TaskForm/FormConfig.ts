
//rather than hardcode numbers, this holds validation information
export const formLimits = {
  taskName: {
    minLength: 4,
    maxlength: 16,
  },
  taskCategory: {
    minLength: 4,
    maxLength: 12,
  },
};
